:- [errors].
:- dynamic notImplemented/0.

%% An encrypted vault %%

% `Vault` is the vault from disk, decrypted using `Password`
openVault(Password, Vault) :- 
    notImplemented.

% True if `Vault` is encrypted and saved to disk
lockVault(Vault) :- 
    notImplemented.